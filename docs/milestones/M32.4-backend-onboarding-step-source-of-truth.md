# Milestone M32.4: Backend Onboarding Steps as Single Source of Truth

## Goal
Make the backend the single source of truth for onboarding steps. Only the backend MUST evaluate the `teamMembers` feature flag for onboarding behavior, and the UI MUST render the onboarding flow strictly from the steps and state returned by the backend.

## Scope
- **Backend (`BuyAlan`)**
  - Refactor onboarding step computation so `teamMembers=false` removes the `invitations` step from backend progression.
  - Ensure backend step normalization, `CurrentStep`, and `CanFinalize` all derive from the same active-step set.
  - Keep the existing onboarding state contract stable where practical.
- **Web API (`BuyAlan.WebApi`)**
  - Align onboarding endpoints with backend step computation so invitation actions are unavailable when `teamMembers=false`.
  - Keep route surface stable; reject disabled invitation actions with deterministic errors instead of removing routes.
- **WebApp (`BuyAlan.WebApp`)**
  - Refactor onboarding rendering so visible steps are derived from backend `steps` and `currentStep`.
  - Remove frontend feature-flag branching from onboarding step visibility, ordering, and progression.
  - Permit frontend `teamMembers` flag usage only for non-step onboarding behavior; it MUST NOT decide whether a step exists, is visible, is current, or is navigable.
  - Do not return payload data for disabled onboarding steps.
- **Tests (`BuyAlan.Tests` / `BuyAlan.WebApi.Tests`)**
  - Add regression coverage for recompute behavior, endpoint behavior, and response consistency when the flag is on and off.
- **WebApp Tests (`BuyAlan.WebApp`)**
  - Add regression coverage for backend-driven onboarding rendering when invitations is present and absent.

## Non-Goals (Out of Scope)
- Changing the `FEATURE_FLAGS` contract or feature-flag service design.
- Database schema or migration changes.
- Removing invitation-related routes from the API surface.
- Changing generated API clients in this milestone.

## User Decisions (Locked)
- [x] The backend MUST be the only place that checks the `teamMembers` flag for onboarding flow.
- [x] The UI SHOULD derive the onboarding flow from backend-provided steps and state instead of checking a frontend feature flag.
- [x] Frontend feature flags MAY still be used in onboarding only for behavior unrelated to deciding whether a step exists, is visible, is current, or is navigable.
- [x] Invitation-specific endpoints SHOULD stay registered but MUST reject requests consistently when the feature is disabled.
- [x] When an onboarding step is disabled, the backend response MUST NOT include payload data for that step.
- [x] Disabled onboarding steps MUST be completely absent from the UI, not shown with disabled-feature messaging.

## Findings from Repository Analysis
- [x] `SubscriptionOnboardingService` is already the canonical place for onboarding progression:
  - [x] `ComputeStateAsync` derives per-step state, `CurrentStep`, and `CanFinalize`.
  - [x] `RecomputeStateAsync` persists normalized onboarding state after recomputation.
- [x] Onboarding step definitions currently always include `invitations`, and helper methods use the static definition list directly.
- [x] `BuyAlan.WebApi/Onboarding/OnboardingEndpoints.cs` still exposes invitation-specific actions:
  - [x] `POST /onboarding/subscriptions/{subscriptionId}/members/invitations`
  - [x] `POST /onboarding/subscriptions/{subscriptionId}/members/invitations/complete`
- [x] `ToEndpointResultAsync` always builds invitation step enrichment, even though step presence should be backend-driven.
- [x] `teamMembers` already exists in the backend feature flag service and defaults to `true`.
- [x] `BuyAlan.WebApp/src/app/onboarding/page.tsx` currently hard-codes a numeric onboarding flow and maps backend steps into fixed UI step numbers.
- [x] The onboarding page currently uses the frontend `teamMembers` feature flag to decide whether to render invitations versus disabled-feature messaging.
- [x] The onboarding page currently treats invitation payload data as available UI state even when step presence should instead come from `steps`.

## Architecture Decisions (Locked)
- [x] `SubscriptionOnboardingService` remains the single place that decides which onboarding steps are active.
- [x] Runtime progression logic MUST use an active step list filtered by `teamMembers`.
- [x] When `teamMembers=false`, backend onboarding MUST behave as if `invitations` does not exist.
- [x] `state.steps` is the authoritative signal for step presence, and disabled steps MUST have no corresponding payload data in the response.
- [x] Frontend code MUST derive step visibility, ordering, progression, and navigation exclusively from backend `steps` and `currentStep`.
- [x] Frontend use of `teamMembers` during onboarding is allowed only for non-step behavior and MUST NOT decide whether a step exists, is visible, is current, or is navigable.
- [x] Disabled invitation actions MUST fail with a dedicated deterministic backend error such as `onboarding_invitations_disabled`.
- [x] The WebApp onboarding page MUST derive visible steps, active progression, and back/skip behavior from backend step order rather than hard-coded step numbers.
- [x] When `invitations` is absent from backend `steps`, the WebApp MUST not render invitation suggestions, invite forms, pending invitations, current members, invitation helper text, or disabled-feature copy.

## Gate A: Backend Step Computation Refactor (Self-Contained)
- [ ] Inject `IFeatureFlagService` into `SubscriptionOnboardingService`.
- [ ] Introduce a private helper that returns the active onboarding step definitions based on `teamMembers`.
- [ ] Refactor `ComputeStateAsync` to use the active step list instead of the static step definition array directly.
- [ ] Refactor step-order helpers to use the active step list:
  - [ ] `GetStepIndex`
  - [ ] `ResolveCurrentStep`
  - [ ] `ResolveNextStepAfterIndex`
  - [ ] `HasCompletedLaterStep`
- [ ] Ensure `BuildStateResult` emits only active steps.
- [ ] Ensure `RecomputeStateAsync` normalizes persisted `CurrentStep` values that point to inactive steps.

### Gate A Acceptance Criteria
- [ ] When `teamMembers=false`, `state.steps` does not contain `invitations`.
- [ ] When `teamMembers=false`, `state.currentStep` is never `invitations`.
- [ ] When persisted state contains `CurrentStep="invitations"` and the flag is off, recompute normalizes to the first valid unresolved active step.
- [ ] When `teamMembers=true`, onboarding progression is unchanged.

## Gate B: Finalize and Service-Level Invitations Behavior (Self-Contained)
- [ ] Update finalize eligibility computation so `CanFinalize` depends only on active required steps.
- [ ] Ensure `FinalizeAsync` works unchanged on top of the recomputed active-step state.
- [ ] Update `CompleteInvitationsAsync` so it returns a deterministic disabled error when `teamMembers=false`.
- [ ] Keep disabled invitations semantically unavailable, not blocked or incomplete.
- [ ] Add or update service-level error codes/messages needed for disabled invitation behavior.

### Gate B Acceptance Criteria
- [ ] When `teamMembers=false`, `CanFinalize` ignores `invitations`.
- [ ] `FinalizeAsync` succeeds once the remaining active required steps are complete.
- [ ] `CompleteInvitationsAsync` returns `onboarding_invitations_disabled` when the feature is off.
- [ ] Existing enabled-path invitation/finalize behavior remains unchanged.

## Gate C: WebAPI Endpoint Consistency (Self-Contained)
- [ ] Update onboarding invitation endpoints in `BuyAlan.WebApi/Onboarding/OnboardingEndpoints.cs` to use backend onboarding step availability as the single authority for disabled-invitation behavior.
- [ ] Keep feature-flag evaluation out of WebApp step rendering and out of endpoint-local branching that bypasses onboarding availability rules.
- [ ] For `POST /members/invitations`, return `onboarding_invitations_disabled` when `teamMembers=false`.
- [ ] For `POST /members/invitations/complete`, return `onboarding_invitations_disabled` when `teamMembers=false`.
- [ ] Extend endpoint error mapping with a dedicated message for the disabled invitation case.
- [ ] Keep the disabled-invitations HTTP response shape compatible with existing onboarding error handling.
- [ ] Keep route registration unchanged so consumers receive deterministic API behavior rather than missing routes.

### Gate C Acceptance Criteria
- [ ] Invitation mutation endpoints reject requests consistently when the feature is off.
- [ ] Endpoint status codes and error payloads remain deterministic and compatible with existing error handling.
- [ ] Enabled-path invitation endpoints keep current behavior.

## Gate D: Response Enrichment and Backend-to-UI Contract Cleanup (Self-Contained)
- [ ] Update `ToEndpointResultAsync` so invitation enrichment is aligned with backend step availability.
- [ ] When `teamMembers=false`, do not build or return invitations payload data.
- [ ] Avoid unnecessary invitation-specific backend work when the feature is off.
- [ ] Document in milestone notes/tests that `steps` is the authoritative source for step existence and that disabled steps have no payload data.

### Gate D Acceptance Criteria
- [ ] Backend onboarding responses make `steps` authoritative and omit payloads for disabled steps.
- [ ] When `teamMembers=false`, responses do not expose invitation-step payload data.
- [ ] The UI can infer onboarding flow from backend state without checking a frontend feature flag.

## Gate E: Regression Tests (Self-Contained)
- [ ] Add service tests covering recompute behavior with `teamMembers=true` and `teamMembers=false`.
- [ ] Add tests for persisted `CurrentStep="invitations"` normalizing correctly when the feature is off.
- [ ] Add tests proving `CanFinalize` ignores invitations when disabled.
- [ ] Add tests for `CompleteInvitationsAsync` returning `onboarding_invitations_disabled` when disabled.
- [ ] Add WebAPI tests for:
  - [ ] `GET /onboarding/.../state` omits the invitations step when disabled.
  - [ ] `POST /members/invitations` rejects when disabled.
  - [ ] `POST /members/invitations/complete` rejects when disabled.
  - [ ] Invitation payload data is absent when disabled.

### Gate E Acceptance Criteria
- [ ] Automated tests protect both enabled and disabled behavior.
- [ ] Regressions in backend step computation and endpoint consistency are caught by tests.

## Gate F: WebApp Backend-Driven Onboarding Rendering (Self-Contained)
- [ ] Refactor `BuyAlan.WebApp/src/app/onboarding/page.tsx` so onboarding step visibility is derived from backend `state.steps` and `state.currentStep`.
- [ ] Remove onboarding page reliance on the frontend `teamMembers` feature flag for deciding whether a step exists, is visible, is current, or is navigable.
- [ ] Replace hard-coded step progression assumptions with backend-driven active-step progression.
- [ ] Ensure the invitations UI renders only when the backend includes the `invitations` step.
- [ ] When invitations is absent from backend steps, do not render invitation suggestions, invite form, pending invitations, current members, invitation helper text, or disabled-feature messaging.
- [ ] Keep the onboarding completion flow working when the backend moves directly from channels to finalize.
- [ ] Ensure the page does not read or require invitations payload data when the backend omits the `invitations` step.
- [ ] Preserve any remaining frontend feature-flag usage only where it controls non-step onboarding behavior.
- [ ] Preserve existing profile/channels prefills and post-finalize success flow while moving render decisions to backend state.
- [ ] Replace numeric next/back/skip assumptions with helpers that move through the active backend step list.

### Gate F Acceptance Criteria
- [ ] When backend `steps` includes `invitations`, the page renders the invitation UI and current invitation flow continues to work.
- [ ] When backend `steps` omits `invitations`, the page does not mention invitations anywhere in the onboarding UI.
- [ ] When backend `currentStep` is `finalize` immediately after channels, the page renders finalize/finish behavior without requiring an invitation step.
- [ ] WebApp onboarding rendering no longer depends on the frontend `teamMembers` feature flag.

## Public Interfaces / Contracts
- [ ] Keep the onboarding route surface unchanged.
- [ ] Add one deterministic backend error code:
  - [ ] `onboarding_invitations_disabled`
- [ ] Keep the onboarding response shape stable where practical.
- [ ] Change backend semantics so:
  - [ ] `steps` is authoritative for which onboarding steps exist.
  - [ ] `currentStep` never references inactive steps.
  - [ ] disabled steps have no corresponding payload data in the response.
- [ ] Change WebApp semantics so:
  - [ ] onboarding rendering follows backend `steps` order rather than a hard-coded local step sequence.
  - [ ] step-specific payload data is read only for steps that exist in backend `steps`.
  - [ ] frontend feature flags MAY still influence non-step onboarding behavior, but MUST NOT decide whether a step exists, is visible, is current, or is navigable.

## Test Scenarios (Authoritative)
1. **Flag enabled happy path**
   - [ ] `teamMembers=true` returns the existing onboarding steps including `invitations`.
   - [ ] Invitation completion and finalize behavior remain unchanged.
2. **Flag disabled recompute path**
   - [ ] `teamMembers=false` omits `invitations` from `steps`.
   - [ ] `CurrentStep="invitations"` is normalized to the first valid unresolved active step.
   - [ ] `CanFinalize` is `true` once remaining active required steps are complete.
3. **Flag disabled invitation action path**
   - [ ] Invitation creation endpoint returns `onboarding_invitations_disabled`.
   - [ ] Invitation completion endpoint returns `onboarding_invitations_disabled`.
4. **Flag disabled response-shape path**
   - [ ] Onboarding response omits payload data for disabled steps.
   - [ ] Invitation payload data is absent and does not contradict `steps`.
5. **WebApp backend-driven rendering path**
   - [ ] When backend `steps` includes `invitations`, the onboarding page renders invitation suggestions, invite form, pending invitations, and current members.
   - [ ] When backend `steps` omits `invitations`, the onboarding page renders neither invitation UI nor disabled-feature placeholder copy.
   - [ ] When backend progresses directly from `channels` to `finalize`, the page still allows onboarding completion.
   - [ ] Compatibility invitation payload data does not cause invitation UI to render unless `steps` includes `invitations`.
   - [ ] Any remaining frontend `teamMembers` flag usage does not affect whether onboarding steps exist, render, or navigate.

## Risks and Mitigations
- [ ] Risk: helper methods partially keep using the static step list, causing inconsistent indexes and step normalization.
  - [ ] Mitigation: make all progression helpers consume the same active step list.
- [ ] Risk: WebAPI still exposes invitation behavior even after recompute removes the step.
  - [ ] Mitigation: explicitly gate invitation endpoints and endpoint enrichment.
- [ ] Risk: UI keeps relying on `invitations` payload presence instead of `steps`.
  - [ ] Mitigation: document and test that `steps` is the authoritative contract for step existence.
- [ ] Risk: the WebApp keeps numeric step assumptions for skip/back/success states, causing broken navigation when `invitations` is removed.
  - [ ] Mitigation: derive render order and transitions from backend active steps and cover direct `channels -> finalize` progression in tests.

## Handoff Notes for New Context Window
- [ ] Start in `BuyAlan/Onboarding/SubscriptionOnboardingService.cs`; Gate A is the foundation for everything else.
- [ ] After backend gates, move to `BuyAlan.WebApp/src/app/onboarding/page.tsx` and remove local feature-flag and numeric-step assumptions.
- [ ] Do not change API route shapes unless a narrower fix is impossible.
- [ ] Treat `steps` and `currentStep` as the canonical output contract for onboarding flow.
- [ ] After any WebAPI contract-affecting change, hand off for openapi client regeneration per repository rules.
